services:
  db:
    image: postgres:17-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: imyanya
      POSTGRES_PASSWORD: imyanya
      POSTGRES_DB: imyanya_tickets
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
